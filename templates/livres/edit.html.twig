{% extends 'base.html.twig' %}

{% block title %}Edit Book - {{ livre.titre }}{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h2>Edit Book "{{ livre.titre }}"</h2>

       {{ form_start(form) }}
    {{ form_widget(form) }}
{{ form_end(form) }}

        <a href="{{ path('admin_livres') }}" class="btn btn-link mt-3">Back to list</a>
    </div>
{% endblock %}
